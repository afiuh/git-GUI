以下是根据我们全部讨论内容整理的**完整需求文档**，涵盖克隆仓库、变更管理、快照管理、分支管理、远程同步以及设计风格要求。你可以将此文档直接交给开发团队或 AI 编码工具作为实现依据。

---

```markdown
# Git GUI 完整需求文档

## 1. 产品概述

一款面向开发者的 Git 图形化客户端，运行在 Web 浏览器中，通过本地后端服务与 Git 仓库交互。核心目标是提供**简洁、清晰、美观、功能全面**的操作体验，覆盖日常 Git 使用的完整流程：从克隆仓库、查看状态、暂存/提交、管理分支、查看历史到推送/拉取远程更新。

## 2. 用户角色

- 开发者：使用 Git 进行版本控制的个人或团队成员。

## 3. 核心功能模块

### 3.1 克隆仓库（Clone）

**功能描述**：首次使用或需要添加新仓库时，从远程仓库克隆代码到本地。

**界面元素**：
- 打开工具时，如果没有打开任何仓库，显示克隆向导（全屏居中卡片）。
- 已打开仓库时，可通过顶部菜单或按钮触发“克隆新仓库”。

**输入项**：
- 远程仓库 URL（支持 HTTPS/SSH）
- 本地存放路径（提供文件夹选择器，默认建议路径如 `~/Documents/<repo-name>`）

**操作流程**：
1. 用户填写 URL 和本地路径。
2. 点击“克隆”按钮。
3. 后端执行 `git clone <url> <path>`，显示克隆进度（可选，第一版可简化）。
4. 克隆成功后自动切换到该仓库，主界面显示三栏视图。

**错误处理**：
- URL 无效、网络错误、权限不足、目标路径已存在非空目录 → 显示红色错误提示。

**对应命令**：
```bash
git clone <url> <path>
```

---

### 3.2 变更管理（文件状态）

**功能描述**：查看工作区相对于最后一次提交的变化，控制哪些变化进入暂存区。

**界面布局**：位于中间栏。

**分组显示**（可折叠）：
- **未暂存的改动**（红色文字）
- **已暂存的改动**（绿色文字）
- **未跟踪的文件**（灰色文字）

每组内列出文件，每个文件包含：
- 文件名（使用状态颜色）
- 操作按钮：
  - 未暂存/未跟踪文件：提供“暂存”按钮（+ 图标）
  - 已暂存文件：提供“取消暂存”按钮（- 图标）
  - 所有文件提供“丢弃修改”按钮（垃圾桶图标，点击需二次确认）

**顶部工具栏**：
- 手动刷新按钮（重新加载文件状态）
- 分组折叠/展开按钮（可选）

**底部区域**：
- 提交信息输入框（多行文本）
- “提交”按钮（主要操作）

**自动刷新**：每次执行暂存、取消暂存、丢弃修改、提交、切换分支、合并等操作后，自动刷新文件列表。

**对应命令**：
- 查看状态：`git status --porcelain`
- 暂存文件：`git add <file>`
- 取消暂存：`git restore --staged <file>`
- 丢弃修改：`git restore <file>`
- 提交：`git commit -m "<message>"`

---

### 3.3 快照管理（提交历史）

**功能描述**：查看历史提交记录，并浏览每次提交的改动内容。

**界面布局**：位于右侧栏。

**提交列表**：
- 每行显示：短哈希（7位，等宽字体）、提交说明、作者、相对时间（如“2小时前”）。
- 默认显示最近 20 条，底部提供“加载更多”按钮。
- 当前分支的最新提交高亮（左侧蓝色竖条或浅色背景）。

**搜索过滤**：顶部搜索框支持按提交说明关键词过滤（前端过滤）。

**点击提交**：在提交列表下方展开详细信息：
- 改动文件列表（每个文件显示状态 M/A/D）
- 点击某个文件后，显示代码差异（diff）：
  - 双栏布局：左侧行号，右侧代码行
  - 添加行：绿色背景 `+` 号
  - 删除行：红色背景 `-` 号
  - 使用等宽字体 JetBrains Mono
  - 顶部显示文件路径，并提供“复制完整 diff”按钮

**对应命令**：
- 查看日志：`git log --oneline --name-status -20`
- 查看 diff：`git show <commit> -- <file>`

---

### 3.4 分支管理

**功能描述**：查看、创建、切换、合并、删除本地分支。

**界面布局**：位于左侧栏。

**分支列表**：
- 显示所有本地分支，当前分支加粗且有背景高亮。
- 鼠标悬停显示该分支的最新提交说明。

**操作按钮**（位于列表上方或每个分支旁）：
- **新建分支**：输入分支名，基于当前 HEAD 创建，并自动切换。
- **切换分支**：双击分支名或点击“切换”按钮。
- **合并到当前**：当前所在分支（如 main）为目标分支，选中另一个分支后点击“合并”。
- **删除分支**：仅当分支已合并且不是当前分支时允许删除。

**切换分支保护**：
- 如果当前有未提交的改动，弹出模态框提示“请先提交或暂存改动”，阻止切换。提供两个选项：“暂存并切换”（自动执行 `git add .` 和切换）、“强制切换（丢弃改动）”（执行 `git checkout --force`，需谨慎，第一版仅提供提示，让用户手动处理）。

**合并冲突处理**：
- 合并时若无冲突，自动完成并提示成功。
- 若产生冲突，在中间栏顶部显示警告卡片：
  - 列出冲突文件。
  - 每个冲突文件提供按钮：“使用我的版本”、“使用他们的版本”、“手动编辑”（手动编辑后用户点击“标记为已解决并继续”）。
  - 用户解决所有冲突后，点击“继续合并”按钮（执行 `git commit` 完成合并）。

**对应命令**：
- 列出分支：`git branch`
- 创建分支：`git switch -c <branch>`
- 切换分支：`git switch <branch>`
- 合并分支：`git merge <branch>`
- 删除分支：`git branch -d <branch>`
- 解决冲突后提交：`git commit`（无需 -m，因为 Git 会生成默认合并信息）

---

### 3.5 远程同步

**功能描述**：与远程仓库交互，拉取更新和推送本地提交。

**界面布局**：位于顶部栏右侧，或以图标按钮形式展示。

**显示信息**：
- 当前远程名称（如 `origin`）和 URL（只读）。
- 状态指示（如“已同步”或“有未拉取更新”）。

**操作按钮**：
- **拉取（Pull）**：执行 `git pull`（fetch + merge），如有冲突则触发合并冲突流程。
- **推送（Push）**：执行 `git push`。若当前分支未设置上游，自动执行 `git push -u origin <当前分支>`。若推送被拒绝（非 fast‑forward），提示“远程有新的提交，请先拉取”。

**可选按钮**：
- **获取（Fetch）**：只下载不合并，第一版可省略。

**认证**：不处理认证细节，依赖 Git 已配置的凭证管理器（SSH key 或 HTTPS 密码缓存）。若认证失败，显示错误信息并建议用户检查命令行配置。

**对应命令**：
- 拉取：`git pull`
- 推送：`git push` 或 `git push -u origin <branch>`

---

## 4. 界面布局与设计风格

### 4.1 整体布局（三栏 + 顶部栏）


+--------------------------+------------------------------+-----------------------------+
|      左侧栏              |         中间栏                |         右侧栏               |
|  （分支管理）            |  （变更管理/文件状态）        |  （快照管理/历史+diff）      |
|                          |                              |                             |
| - 当前分支（高亮）       | - 未暂存改动（红色）          | - 提交列表（短哈希+说明）     |
| - 本地分支列表           | - 已暂存改动（绿色）          | - 点击提交显示：             |
| - 操作按钮：             | - 未跟踪文件（灰色）          |   · 改动文件列表             |
|   · 新建分支             |   · 每个文件带操作按钮：       |   · 文件 diff（双栏）        |
|   · 切换（双击）         |       · 暂存 / 取消暂存        |                             |
|   · 合并到当前           |       · 丢弃修改              |                             |
|   · 删除                 | - 底部：提交信息输入框 + 提交按钮 |                             |
+--------------------------+------------------------------+-----------------------------+
|                        顶部栏（仓库路径、远程状态、刷新、主题切换、克隆按钮）           |
+------------------------------------------------------------------------------


### 4.2 设计系统（基于 Expo，Git 场景适配）

#### 默认浅色主题
- 页面背景：云灰 `#f0f0f3`
- 卡片表面：纯白 `#ffffff`
- 主文字：近黑 `#1c2024`
- 辅助文字：石板灰 `#60646c`
- 边框：边框薰衣草 `#e0e1e6`
- 链接/焦点：链接钴蓝 `#0d74ce`

#### 可选暗色主题
- 页面背景：深空灰 `#121212`
- 卡片表面：暗炭色 `#2c2c2c`
- 主文字：浅灰 `#e5e5e5`
- 辅助文字：银灰 `#a0a0a0`
- 边框：暗石板 `#363a3f`
- 链接/焦点：亮钴蓝 `#3b82f6`

#### Git 专属颜色（仅用于状态标记）
| 含义 | 浅色 | 暗色 |
|------|------|------|
| 未暂存 | `#e5484d` | `#f87171` |
| 已暂存 | `#2e7d32` | `#4ade80` |
| 未跟踪 | `#6c6f78` | `#9ca3af` |
| 冲突背景 | `#fef5e7` + 边框 `#ab6400` | `#332a1a` + 边框 `#f59e0b` |
| Diff 添加行 | 背景 `#e6f4ea`，文字 `#1e4620` | 背景 `#1a3b2c`，文字 `#bbf7d0` |
| Diff 删除行 | 背景 `#fce8e8`，文字 `#8b3a3a` | 背景 `#3b2626`，文字 `#fecaca` |

#### 字体
- 主字体：Inter（400–900 字重）
- 等宽字体：JetBrains Mono（用于哈希、diff、代码块）

#### 圆角与阴影
- 卡片圆角：8px
- 按钮圆角：6px（普通），9999px（主要药丸形）
- 阴影：轻语阴影（等级 2）用于卡片，无重阴影

#### 间距
- 三栏间距：24px
- 卡片内边距：24px
- 文件行高：48px
- 提交历史行高：56px

### 4.3 响应式行为
- 桌面（>1024px）：完整三栏布局。
- 平板（640–1024px）：右侧栏折叠为抽屉，点击提交时滑出。
- 移动端（<640px）：只显示中间栏，左右栏通过底部浮动按钮呼出。

---

## 5. 交互与反馈规范

- **异步操作**：提交、推送、拉取、合并、克隆等显示顶部进度条，同时禁用相关按钮。
- **成功反馈**：右下角 toast 提示（绿色边框，3秒消失）。
- **错误反馈**：红色边框弹窗，显示原始 Git 错误信息（可折叠详情），提供“复制错误”按钮。
- **空状态**：当没有改动时，中间栏显示插画和文字：“工作区干净，没有什么需要提交的”。

---

## 6. 技术实现建议

### 6.1 架构
- **前端**：React + Vite + Tailwind CSS + shadcn/ui（按 Expo 风格定制）
- **后端**：Node.js + Express + simple-git（提供 REST API）
- **通信**：前端通过 HTTP 请求调用后端 API。
- **启动方式**：用户双击 `start.bat`，同时启动后端服务和浏览器打开 `http://localhost:3000`。

### 6.2 后端 API 设计（REST）

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/clone` | POST | 克隆仓库（body: `{ url, path }`） |
| `/api/status` | GET | 返回 `git status --porcelain` 解析结果 |
| `/api/add` | POST | 暂存文件（body: `{ files: string[] }`） |
| `/api/restore` | POST | 取消暂存或丢弃修改（body: `{ files: string[], staged: boolean }`） |
| `/api/commit` | POST | 提交（body: `{ message: string }`） |
| `/api/log` | GET | 获取提交历史（参数 `?n=20&search=keyword`） |
| `/api/show` | GET | 获取某提交的某文件 diff（参数 `?commit=<hash>&file=<path>`） |
| `/api/branches` | GET | 获取本地分支列表 |
| `/api/branch/new` | POST | 创建分支（body: `{ name: string }`） |
| `/api/branch/switch` | POST | 切换分支（body: `{ name: string }`） |
| `/api/branch/merge` | POST | 合并分支（body: `{ fromBranch: string }`） |
| `/api/branch/delete` | DELETE | 删除分支（body: `{ name: string }`） |
| `/api/pull` | POST | 拉取远程更新 |
| `/api/push` | POST | 推送当前分支 |
| `/api/repo/info` | GET | 返回当前仓库路径、远程 URL、当前分支等 |

### 6.3 设计令牌（CSS 变量）

在 Expo 基础上定义以下变量，支持浅色/暗色切换：

```css
:root {
  --bg-page: #f0f0f3;
  --bg-card: #ffffff;
  --text-primary: #1c2024;
  --text-secondary: #60646c;
  --border: #e0e1e6;
  --link: #0d74ce;
  --git-red: #e5484d;
  --git-green: #2e7d32;
  --git-gray: #6c6f78;
  --git-warning: #ab6400;
  --git-add-bg: #e6f4ea;
  --git-add-text: #1e4620;
  --git-remove-bg: #fce8e8;
  --git-remove-text: #8b3a3a;
  --diff-line-number: #b0b4ba;
}

[data-theme="dark"] {
  --bg-page: #121212;
  --bg-card: #2c2c2c;
  --text-primary: #e5e5e5;
  --text-secondary: #a0a0a0;
  --border: #363a3f;
  --link: #3b82f6;
  --git-red: #f87171;
  --git-green: #4ade80;
  --git-gray: #9ca3af;
  --git-warning: #f59e0b;
  --git-add-bg: #1a3b2c;
  --git-add-text: #bbf7d0;
  --git-remove-bg: #3b2626;
  --git-remove-text: #fecaca;
  --diff-line-number: #6b7280;
}
```

---

## 7. 非功能性需求

- **性能**：对于大型仓库（10 万+文件），`git status` 可能较慢，需显示 loading 动画，避免界面假死。
- **安全性**：后端只允许访问本地文件系统，不暴露给外部网络（绑定 `localhost`）。
- **错误恢复**：如果 Git 命令超时或意外退出，应给出明确提示，并允许用户重试。
- **可扩展性**：模块化设计，便于后续增加 stash、rebase、标签管理等功能。

---

## 8. 版本规划（第一版范围）

### 必须实现（MVP）
- 克隆仓库
- 变更管理（查看状态、暂存/取消暂存/丢弃、提交）
- 快照管理（日志列表、查看 diff）
- 分支管理（列表、新建、切换、合并、删除）
- 远程同步（拉取、推送）
- 浅色/暗色主题切换
- 基本的错误处理和反馈

### 暂不实现（后续版本）
- 交互式 rebase
- stash 管理
- 子模块支持
- 图形化分支图
- 远程分支管理等

---

## 9. 文档更新记录

| 版本 | 日期 | 修改内容 |
|------|------|----------|
| 1.0 | 2025-04-24 | 初始完整需求文档，包含克隆、变更、快照、分支、远程同步及设计规范 |

---

请开发团队严格按照本文档执行编码。如有需求变更，需更新本文档并通知所有相关人员。
```

---

以上即为完整的需求文档。你可以保存为 `REQUIREMENTS.md` 并开始开发。如果还需要补充任何细节，请随时提出。